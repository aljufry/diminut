<% @page_title = t(:new_subnet) %>

<div class="pageTitle">
  <%= t(:new_subnet) %>
</div>

<div class="form">
  <%= form_for(:subnet, :url => {:action => 'create'}) do |f| %>
  <% if @subnet.errors.any? %>
  
  <div class="error">
    <ul>
      <% @subnet.errors.full_messages.each do |message| %>
      <li>
        <%= message %>
      </li>
      <% end %>
    </ul>
  </div>
  <% end %>
  <%= render(:partial => "form", :locals => {:f => f}) %>
  <% end %>
</div>
